<%= turbo_stream.replace "modal", partial: "shared/modal", locals: {
  title: "New Book",
  content: render("admin/books/form", book: @book),
  footer: "<button class='btn btn-secondary' data-action='modal#close'>Close</button>"
} %>