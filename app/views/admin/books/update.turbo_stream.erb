<% if @book.errors.empty? %>
  <%= close_modal %>
  <%= turbo_stream.replace "admin_book_#{@book.id}", @book %>
  <%= turbo_stream.prepend "flash", partial: "shared/flash" %>
<% else %>
  <%= open_modal("Edit Book", render("admin/books/form", book: @book)) %>
<% end %>
